rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporaire : permettre tous les accès
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
